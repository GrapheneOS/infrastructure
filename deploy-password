#!/bin/bash

. shared.sh
. hosts.sh

for host in ${hosts_all[@]}; do
    remote=root@$host

    echo
    echo $host
    echo

    password=$(head -c22 <(tr -dc A-Za-z0-9 </dev/random))
    echo $password > passwords/$host
    ssh $remote "echo root:$password | chpasswd"
done
